struct Commands {}

impl Commands {
    pub fn query_orders() {}
}

struct Notifiers {}

impl Notifiers {
    // Registration functions - async events, message inbound, completions?

    pub fn register_notifier() {
        // handler signature - needs to pertain to an order # minimally
    }
}
